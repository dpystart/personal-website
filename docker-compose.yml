services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - backend
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - SCRIPTS_DIR=/mydata/scripts
      - PORT=3001
    volumes:
      - /mydata/scripts:/mydata/scripts
    restart: unless-stopped

